@model RealEstateProject.Models.Classes.Message
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Us</title>
    <link rel="stylesheet" href="~/estate/style.css"> <!-- Genel CSS -->
    <link rel="stylesheet" href="~/estate/contact.css"> <!-- Contact sayfasına özel CSS -->
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar">
        <ul>
            <li><a href="/MainPage/Index">Home</a></li>
            <li><a href="/Project/Index">Projects</a></li>
            <li><a href="/MainPage/Index#listings">Listings</a></li>
            <li><a href="#" class="active">Contact</a></li>
        </ul>
    </nav>

    <!-- Main Content -->
    <main class="contact-container">
        <!-- Left: Offices -->
        <section class="offices">
            @Html.Action("Office","Contact")

        </section>


        <!-- Right: Contact Form -->
        <section class="contact-form">
            <h2>Get in Touch</h2>
            <form method="post">
                <label for="name">Full Name</label>
                @Html.TextBoxFor(x => x.fullName, new { @class = "form-control", @id = "name", @placeholder = "Enter your full name" })
                @*<input type="text" id="name" placeholder="Enter your full name" required>*@

                <label for="phone">Phone Number</label>
                @Html.TextBoxFor(x => x.phoneNumber, new { @class = "form-control", @id = "phone", @placeholder = "Enter your phone number " })
                @*<input type="tel" id="phone" placeholder="Enter your phone number" required>*@

                <label for="email">E-Mail</label>
                @Html.TextAreaFor(x => x.email, new { @class = "form-control", @id = "enail", @placeholder = "Enter your email adress" })

                <label for="message">Message</label>
                @Html.TextAreaFor(x => x.message, new { @class = "form-control", @id = "message", @placeholder = "Enter your message " ,@row="4",@column="30"})
                @*<textarea id="message" placeholder="Write your message here" rows="5" required></textarea>*@

                <button type="submit">Send Message</button>
            </form>
        </section>
    </main>

    <!-- Contact Section -->
    <section class="contact">
        <div class="contact-wrapper">
            <div class="company-logo">
                <img src="~/estate/imgs/logo.jpg" alt="Company Logo">
            </div>
            @Html.Action("Contact","MainPage")
        </div>
    </section>

</body>
</html>

